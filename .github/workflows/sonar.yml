# .github/workflows/sonar.yml
name: Sonar Scan

on:
 workflow_call:

jobs:
  sonar:
    runs-on: java_build
    #needs: build  # Ensures Sonar runs after build succeeds
    steps:
      - name: SonarCloud Scan
        run: mvn sonar:sonar -Dsonar.projectKey=shaazAhmed91_DevSecops_Pipeline -Dsonar.organization=shaazAhmed91 -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=e385a10791f9d169f3a78eef753a57b315f2c12e
